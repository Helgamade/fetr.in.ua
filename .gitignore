# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# User uploads — persistent data, never in Git
uploads/

# Environment secrets — never in Git
server/.env
.env

# Compiled index.html — генерируется при деплое из index.src.html
# index.src.html — исходник (в Git), index.html — скомпилированный (НЕ в Git)
index.html
