# 📋 Детальне пояснення процесу Prerender

**Дата створення:** 2026-01-27  
**Версія:** 1.0

---

## 🕐 1. КОЛИ ЗАПУСКАЄТЬСЯ PRERENDER?

### ✅ Відповідь: Під час `npm run build` (автоматично)

**Послідовність виконання:**

```bash
npm run build
  ↓
1. cross-env VITE_PRERENDER=1 vite build
   └─> Збирає React додаток в dist/
   └─> Встановлює VITE_PRERENDER=1 для умовного рендерингу
   
2. npm run postbuild
   └─> cross-env VITE_PRERENDER=1 node scripts/prerender.js
       └─> Запускає Puppeteer
       └─> Генерує статичний HTML для кожного роуту
```

**Ключові моменти:**
- ✅ Prerender запускається **АВТОМАТИЧНО** після збірки
- ✅ Виконується **ЛОКАЛЬНО** на машині розробника/CI
- ✅ **НЕ** запускається на сервері після деплою
- ✅ **НЕ** запускається окремою командою (тільки через `npm run build`)

**Файли:**
- `package.json` (рядки 8-9):
  ```json
  "build": "cross-env VITE_PRERENDER=1 vite build && npm run postbuild",
  "postbuild": "cross-env VITE_PRERENDER=1 node scripts/prerender.js"
  ```

---

## 📊 2. ЗВІДКИ БЕРУТЬСЯ ДАНІ ДЛЯ PRERENDER?

### ✅ Відповідь: З **ЖИВОГО API (MySQL)** на момент білду

**Процес отримання даних:**

1. **Prerender запускає локальний Express сервер:**
   ```javascript
   // scripts/prerender.js (рядки 29-34)
   const app = express();
   app.use(express.static(DIST_PATH)); // Віддає статичні файли з dist/
   app.use((req, res) => {
     res.sendFile(join(DIST_PATH, 'index.html')); // SPA fallback
   });
   ```

2. **Puppeteer відкриває браузер і переходить на URL:**
   ```javascript
   await page.goto(`http://localhost:3333${route}`);
   ```

3. **React додаток завантажується в браузері:**
   - Виконується JavaScript
   - React Router визначає роут (`/privacy-policy`)
   - Компонент `Page.tsx` рендериться

4. **Компонент `Page.tsx` робить API запит:**
   ```typescript
   // src/pages/Page.tsx (рядок 16)
   const { data: page, isLoading, error } = usePage(slug || '');
   ```

5. **Hook `usePage` викликає API:**
   ```typescript
   // src/hooks/usePages.ts (рядки 24-30)
   export function usePage(slug: string) {
     return useQuery<Page>({
       queryKey: ['page', slug],
       queryFn: () => pagesAPI.getBySlug(slug), // ← API виклик
       enabled: !!slug,
     });
   }
   ```

6. **API запит йде на Express сервер:**
   ```typescript
   // src/lib/api.ts (рядок 274)
   getBySlug: (slug: string) => fetchAPI<any>(`/pages/${slug}`)
   ```

7. **Express сервер робить запит до MySQL:**
   ```javascript
   // server/routes/pages.js (рядки 19-34)
   router.get('/:slug', async (req, res, next) => {
     const [pages] = await pool.execute(`
       SELECT * FROM pages WHERE slug = ? AND is_published = TRUE
     `, [slug]);
     res.json(pages[0]);
   });
   ```

**⚠️ КРИТИЧНО:**
- Дані беруться з **ЖИВОГО MySQL** на момент білду
- **НЕ** використовуються моки
- **НЕ** використовуються статичні файли
- API сервер **МАЄ БУТИ ЗАПУЩЕНИМ** під час prerender!

**Як це працює:**
- Prerender запускає локальний Express сервер на `localhost:3333`
- Цей сервер віддає статичні файли з `dist/`
- Але React додаток всередині робить API запити до **production API** (`https://fetr.in.ua/api/...`)
- Або до локального API, якщо він запущений на порту 3000

---

## 📅 3. ЯКА ВЕРСІЯ КОНТЕНТУ ПОТРАПЛЯЄ В HTML?

### ✅ Відповідь: **Актуальна з БД на момент білду**

**Процес:**

1. **Момент білду:**
   - Розробник/CI виконує `npm run build`
   - Prerender запускається
   - Puppeteer відкриває браузер
   - React робить API запит до MySQL
   - MySQL повертає **поточний** контент з БД

2. **HTML зберігається:**
   ```javascript
   // scripts/prerender.js (рядки 88-101)
   const html = await page.content(); // Отримує повний HTML
   writeFileSync(filePath, html); // Зберігає в dist/privacy-policy/index.html
   ```

3. **Результат:**
   - HTML містить контент, який був в БД **на момент білду**
   - Якщо контент змінився в БД **ПІСЛЯ** білду → HTML **НЕ** оновиться автоматично

**⚠️ ВАЖЛИВО:**
- Prerendered HTML = "сніпшот" стану БД на момент білду
- Якщо змінити контент в адмінці → треба **пересобрати** (`npm run build`)
- Без пересборки боти бачать **застарілий** контент

---

## 🚨 4. ЩО ВІДБУВАЄТЬСЯ, ЯКЩО API/БД НЕДОСТУПНІ?

### ⚠️ Відповідь: **Prerender продовжує роботу, але HTML буде порожнім або з помилкою**

**Сценарії:**

### Сценарій A: API недоступний (сервер не запущений)

```javascript
// src/pages/Page.tsx (рядки 18-24)
if (isLoading) {
  return <Loader2 className="h-8 w-8 animate-spin" />; // ← Показується loader
}

// Prerender чекає на data-prerender-ready marker
// Але маркер встановлюється після завантаження даних
// Якщо API не відповідає → маркер НЕ встановлюється
// Prerender чекає 10 секунд (timeout) → продовжує → зберігає HTML з loader
```

**Результат:** HTML містить `<Loader2>` компонент замість контенту

### Сценарій B: MySQL недоступний

```javascript
// server/routes/pages.js (рядки 19-34)
router.get('/:slug', async (req, res, next) => {
  try {
    const [pages] = await pool.execute(`SELECT * FROM pages...`);
    // Якщо MySQL недоступний → помилка
  } catch (error) {
    next(error); // ← Помилка передається далі
  }
});
```

**Результат:**
- API повертає 500/503 помилку
- React показує error state (рядки 26-51 в Page.tsx)
- HTML містить "Сторінку не знайдено" або порожній контент

### Сценарій C: Сторінка не знайдена в БД

```javascript
// server/routes/pages.js (рядки 26-28)
if (pages.length === 0) {
  return res.status(404).json({ error: 'Page not found' });
}
```

**Результат:**
- React показує error state
- HTML містить "Сторінку не знайдено"

**⚠️ КРИТИЧНА ПРОБЛЕМА:**
- Prerender **НЕ падає** при помилках API
- Він просто зберігає HTML з помилкою/loader
- Боти можуть отримати порожній або неправильний HTML!

**Рішення:**
- ✅ Завжди перевіряти, що API сервер запущений перед білдом
- ✅ Перевіряти, що MySQL доступний
- ✅ Перевіряти, що всі сторінки існують в БД перед prerender

---

## 🔄 5. АВТОМАТИЧНЕ ОНОВЛЕННЯ PRERENDER?

### ❌ Відповідь: **НІ, потрібен ручний ребілд**

**Поточна ситуація:**

1. **Зміна контенту в адмінці:**
   - Адміністратор змінює контент через `/admin/pages`
   - Дані зберігаються в MySQL
   - Prerendered HTML **НЕ** оновлюється автоматично

2. **Щоб оновити prerendered HTML:**
   ```bash
   npm run build  # ← Ручна команда
   git push       # ← Ручний деплой
   ```

**⚠️ ПРОБЛЕМА:**
- Якщо змінити контент в адмінці → prerendered HTML залишається **застарілим**
- Боти бачать старий контент до наступного білду
- Google Ads може отримати застарілу інформацію

**Рекомендації (з SEO_CHECKLIST_RESULTS.md):**

### Варіант A: Кнопка "Оновити SEO-версію"
- В адмінці кнопка → webhook → GitHub Actions → автоматичний build+deploy

### Варіант B: Авто-тригер після збереження
- POST `/api/pages/:slug` → webhook → автоматичний build+deploy

### Варіант C: Hybrid підхід
- Policy pages (рідко змінюються) → ребілд вручну 1 раз на місяць
- Блог/новини (часто) → авто-ребілд після публікації

**Зараз:** Жоден з варіантів **НЕ** реалізований

---

## 📋 6. ПОВНИЙ СПИСОК URL ДЛЯ PRERENDER

### ✅ Поточний список (scripts/prerender.js, рядки 14-24):

```javascript
const routes = [
  '/',                      // Головна сторінка
  '/reviews',               // Відгуки
  '/about',                 // Про нас
  '/privacy-policy',        // Політика конфіденційності
  '/terms-of-use',          // Умови використання
  '/dostavka',              // Доставка та оплата
  '/povernennya-tovaru',    // Повернення та обмін
  '/contacts',              // Контактна інформація
];
```

**Всього: 8 сторінок**

**Критичні для Google Ads:**
- ✅ `/privacy-policy` - **ОБОВ'ЯЗКОВО**
- ✅ `/terms-of-use` - **ОБОВ'ЯЗКОВО**
- ✅ `/dostavka` - **РЕКОМЕНДОВАНО**
- ✅ `/povernennya-tovaru` - **РЕКОМЕНДОВАНО**

**Додаткові:**
- ✅ `/about` - для довіри
- ✅ `/contacts` - для контактної інформації
- ✅ `/` - головна
- ✅ `/reviews` - відгуки

---

## 🎯 ВИСНОВКИ ТА РЕКОМЕНДАЦІЇ

### ✅ Що працює добре:

1. **Автоматичний запуск:** Prerender запускається автоматично під час білду
2. **Актуальні дані:** HTML містить актуальний контент з БД на момент білду
3. **Стабільність:** Prerender не падає при помилках (але це може бути проблемою)

### ⚠️ Потенційні проблеми:

1. **Застарілий контент:**
   - Якщо змінити контент в адмінці → треба пересобрати
   - Без пересборки боти бачать старий контент

2. **Ризик порожнього HTML:**
   - Якщо API/MySQL недоступні → HTML містить loader/помилку
   - Боти можуть отримати неправильний контент

3. **Відсутність автоматизації:**
   - Немає автоматичного оновлення після змін в адмінці
   - Потрібен ручний ребілд

### 🔧 Рекомендації:

1. **Додати перевірки перед prerender:**
   ```javascript
   // Перевірити, що API доступний
   // Перевірити, що MySQL доступний
   // Перевірити, що всі сторінки існують в БД
   ```

2. **Додати fallback для помилок:**
   ```javascript
   // Якщо API недоступний → використати останній успішний prerender
   // Або показати попередження в консолі
   ```

3. **Налаштувати автоматичний ребілд:**
   - Webhook після збереження сторінки
   - GitHub Actions для автоматичного build+deploy

4. **Додати моніторинг:**
   - Перевіряти розмір prerendered HTML (не повинен бути < 1KB)
   - Перевіряти наявність контенту в HTML
   - Логувати помилки prerender

---

## 📊 СХЕМА ПРОЦЕСУ PRERENDER

```
npm run build
  │
  ├─> vite build
  │   └─> Збирає React в dist/
  │
  └─> node scripts/prerender.js
      │
      ├─> Запускає Express на localhost:3333
      │   └─> Віддає статичні файли з dist/
      │
      ├─> Запускає Puppeteer (headless браузер)
      │
      └─> Для кожного роуту:
          │
          ├─> page.goto('http://localhost:3333/route')
          │   └─> React завантажується
          │   └─> React Router визначає роут
          │   └─> Компонент рендериться
          │
          ├─> Компонент робить API запит
          │   └─> fetchAPI('/api/pages/slug')
          │       └─> Express сервер (production або local)
          │           └─> MySQL запит
          │               └─> Повертає дані з БД
          │
          ├─> React рендерить контент
          │   └─> Встановлює data-prerender-ready='true'
          │
          ├─> Puppeteer чекає на маркер (10 сек timeout)
          │
          └─> Зберігає HTML в dist/route/index.html
```

---

**Останнє оновлення:** 2026-01-27  
**Автор:** AI Assistant
