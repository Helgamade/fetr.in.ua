# 📦 Полная инструкция по деплою приложения fetr.in.ua

## 🎯 Назначение документа

Эта инструкция предназначена для автоматических агентов и разработчиков, которые выполняют деплой приложения на production сервер.

---

## 🔐 Доступы и подключение

### SSH доступ:
- **Хост:** `idesig02.ftp.tools`
- **Пользователь:** `idesig02`
- **Путь к проекту:** `/home/idesig02/fetr.in.ua/www/`
- **Git репозиторий:** `https://github.com/Helgamade/fetr.in.ua.git`
- **Ветка:** `main`

### Проверка SSH подключения:
```bash
ssh idesig02@idesig02.ftp.tools "echo 'SSH connection successful'"
```

---

## 📋 Полная последовательность деплоя

### Шаг 1: Сборка приложения (локально)

```powershell
# Перейти в директорию проекта
cd E:\fetr.in.ua\www

# Установить зависимости (если нужно)
npm install

# Собрать фронтенд
npm run build
```

**Результат:** Создана папка `dist/` с собранными файлами.

---

### Шаг 2: Коммит и пуш в GitHub

```powershell
# Добавить все изменения (включая dist/ если изменился фронтенд)
git add -A

# Закоммитить с описанием
git commit -m "Описание изменений"

# Загрузить в GitHub
git push origin main
```

**Важно:** Если изменился фронтенд, обязательно включить `dist/` в коммит!

---

### Шаг 3: Деплой на сервер

**Используй ЭТУ команду для деплоя:**

```powershell
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && git fetch origin && git reset --hard origin/main && bash server/deploy.sh"
```

**Что происходит:**
1. Подключение к серверу по SSH
2. Переход в директорию проекта
3. Обновление кода из GitHub (`git fetch` + `git reset --hard`)
4. Запуск скрипта деплоя `server/deploy.sh`

---

## 🔄 Что делает скрипт `server/deploy.sh`

Скрипт автоматически выполняет:

1. **Сборка фронтенда:**
   ```bash
   npm run build
   ```

2. **КРИТИЧНО: Копирование файлов из `dist/` в корень:**
   ```bash
   mkdir -p assets
   cp dist/index.html index.html              # ПЕРВЫМ! Без этого сайт не загрузится!
   cp -r dist/assets/* assets/                # Копирование JS/CSS файлов
   cp dist/favicon.ico favicon.ico            # Если есть
   cp dist/robots.txt robots.txt              # Если есть
   ```

3. **Установка прав доступа (ОБЯЗАТЕЛЬНО!):**
   ```bash
   chmod 755 assets
   chmod 644 assets/*
   chmod 644 index.html
   ```

4. **Проверка деплоя:**
   - Проверяет, что `index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`
   - Проверяет, что папка `assets/` не пустая

5. **Перезапуск сервера:**
   ```bash
   cd server
   pkill -f "node.*index.js"
   sleep 1
   nohup node index.js > /dev/null 2>&1 &
   ```

---

## ✅ Чеклист успешного деплоя

После выполнения деплоя проверь:

- [ ] `npm run build` выполнен успешно (локально)
- [ ] Изменения закоммичены и запушены в GitHub
- [ ] Команда деплоя выполнена без ошибок
- [ ] На сервере `index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`
- [ ] Права доступа установлены: `755` для папок, `644` для файлов
- [ ] Сайт открывается: `https://fetr.in.ua/`
- [ ] Нет ошибок в консоли браузера (F12 → Console)

---

## 🚨 КРИТИЧЕСКИ ВАЖНО

### 1. Репозиторий и рабочая папка ОДНА И ТА ЖЕ
- **Путь:** `/home/idesig02/fetr.in.ua/www/`
- **НЕ использовать:** `/home/idesig02/fetr.in.ua/` (без `/www/`)

### 2. Копирование `index.html` ПЕРВЫМ
- **ВСЕГДА** копировать `dist/index.html` → `index.html` ПЕРВЫМ
- Без этого сайт НЕ ЗАГРУЗИТСЯ!

### 3. Проверка `index.html`
- **Должно быть:** `<script type="module" crossorigin src="/assets/index-*.js"></script>`
- **НЕ должно быть:** `<script type="module" src="/src/main.tsx"></script>`

### 4. Права доступа
- **Папки:** `755` (drwxr-xr-x)
- **Файлы:** `644` (-rw-r--r--)

---

## 🐛 Устранение проблем

### Проблема 1: "Failed to load module script"

**Причина:** На сервере старый `index.html`, который ссылается на `/src/main.tsx`.

**Решение:**
```bash
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && cp dist/index.html index.html && echo '=== INDEX.HTML FIXED ==='"
```

**Проверка:**
```bash
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && grep -E 'main\.tsx|index-.*\.js' index.html"
# Должно быть: /assets/index-*.js
# НЕ должно быть: /src/main.tsx
```

---

### Проблема 2: "403 Forbidden" для CSS/JS файлов

**Причина:** Неправильные права доступа.

**Решение:**
```bash
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && chmod 755 assets && chmod 644 assets/* && chmod 644 index.html && echo '=== PERMISSIONS FIXED ==='"
```

**Проверка:**
```bash
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && ls -la assets/"
# Должно быть: drwxr-xr-x для папки, -rw-r--r-- для файлов
```

---

### Проблема 3: "Permission denied (publickey)"

**Причина:** SSH ключ не настроен.

**Решение:** Сообщить пользователю:
> "Не удалось подключиться к серверу по SSH. Проверьте SSH ключ в `~/.ssh/authorized_keys` на сервере."

---

### Проблема 4: Файлы не обновляются на сайте

**Решение:**
```bash
# 1. Проверить, что файлы обновились на сервере
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && ls -la dist/assets/ | head -5"

# 2. Повторить деплой
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && git fetch origin && git reset --hard origin/main && bash server/deploy.sh"
```

**Сообщить пользователю:**
> "Файлы на сервере обновлены. Очистите кеш браузера (Ctrl+Shift+R)."

---

## 📝 Примеры команд для копирования

### Полная команда деплоя (одна строка):
```powershell
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && git fetch origin && git reset --hard origin/main && bash server/deploy.sh"
```

### Проверка деплоя:
```powershell
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && grep -E 'main\.tsx|index-.*\.js' index.html"
```

### Проверка прав доступа:
```powershell
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && ls -la assets/"
```

### Проверка статуса сервера:
```powershell
ssh idesig02@idesig02.ftp.tools "ps aux | grep 'node.*index.js'"
```

---

## 🔗 Ссылки на дополнительные документы

- **Основные правила деплоя:** `.cursor/rules/deploy-rules.md`
- **Настройка деплоя:** `.cursor/rules/setup-deployment.md`
- **Правила проекта:** `.cursor/rules/project-rules.md`
- **Скрипт деплоя:** `server/deploy.sh`

---

## 📅 Последнее обновление

**Дата:** 2025-01-XX  
**Версия:** 1.0  
**Автор:** AI Assistant

---

## ⚠️ ВАЖНО ДЛЯ АГЕНТОВ

**ВСЕГДА используй эту последовательность:**

1. `npm run build` (локально)
2. `git add -A && git commit -m "..." && git push origin main`
3. `ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && git fetch origin && git reset --hard origin/main && bash server/deploy.sh"`

**НЕ изменяй эту последовательность!**

**НЕ используй другие пути к проекту!**

**НЕ пропускай копирование `index.html`!**

