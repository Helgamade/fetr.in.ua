# Правила проекта FetrInUA

## 🔴 КРИТИЧЕСКИ ВАЖНО

**Принцип разработки:** Использовать ТОЛЬКО проверенные, надёжные решения, которые будут работать у всех разработчиков проекта.

### Запрещено:
- ❌ Использовать "быстрые решения" (workarounds)
- ❌ Использовать нативные alert(), confirm(), prompt() вместо UI компонентов
- ❌ Использовать костыли и временные решения
- ❌ Изменять архитектуру без согласования
- ❌ Загружать файлы вручную через FTP/FileZilla/WinSCP (только через Git!)
- ❌ Коммитить `node_modules`, `.env*` файлы
- ❌ **Писать самописные UI компоненты без письменного подтверждения!** Сначала проверить [shadcn/ui Components](https://ui.shadcn.com/docs/components) и [shadcn/ui Blocks](https://ui.shadcn.com/blocks)

### Обязательно:
- ✅ Использовать существующие библиотеки проекта (shadcn-ui, react-router-dom, react-query)
- ✅ **ПРИОРИТЕТ #1: Использовать готовые компоненты из [shadcn/ui](https://ui.shadcn.com/)**
- ✅ **ПРИОРИТЕТ #2: Использовать готовые блоки из [shadcn/ui blocks](https://ui.shadcn.com/blocks)**
- ✅ Следовать стилю кода существующих компонентов
- ✅ Тестировать решения перед коммитом
- ✅ Документировать изменения в `.cursor/rules/`
- ✅ Деплоить изменения ТОЛЬКО через Git (см. deploy-rules.md)
- ✅ После каждого коммита с изменениями фронтенда выполнять `npm run build` и коммитить `dist/`
- ✅ **После каждого деплоя проверять timestamp в терминале (первая строка после "=== DEPLOYMENT COMPLETE ===")**
- ✅ **Проверять, что timestamp на сайте (`https://fetr.in.ua/DEPLOY_TIMESTAMP.txt`) совпадает с выведенным в терминале**

---

## 🚨🚨🚨 КРИТИЧНО: index.html - ПЕРВЫЙ ПРИОРИТЕТ! 🚨🚨🚨

**ЭТА ОШИБКА ПОВТОРЯЕТСЯ ПОСТОЯННО! ВСЕГДА ПРОВЕРЯЙ ПЕРЕД ОТВЕТОМ!**

### ⚠️ ОБЯЗАТЕЛЬНО ПЕРЕД КАЖДЫМ ОТВЕТОМ:
1. ✅ Прочитать эти правила полностью
2. ✅ После сборки (`npm run build`) ВСЕГДА копировать `dist/index.html` в `www/index.html` на сервере
3. ✅ Проверить, что `www/index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`
4. ✅ В консоли браузера НЕТ ошибки "Failed to load module script: Expected a JavaScript-or-Wasm module script"

### 🚨 КРИТИЧНО: Автоматическое копирование файлов (РЕШЕНО!)

**Git hooks автоматически копируют файлы после каждого git pull/reset:**
- `.git/hooks/post-merge` - после `git pull` или `git merge`
- `.git/hooks/post-checkout` - после `git reset --hard` или `git checkout`

**Оба hook вызывают `server/auto-deploy.sh`, который:**
1. Копирует `dist/index.html` → `index.html` (ПЕРВЫМ!)
2. Копирует `dist/assets/*` → `assets/`
3. Проверяет, что файлы скопированы правильно (нет ссылок на `main.tsx`)
4. Устанавливает права доступа

**Эта ошибка больше НЕ ДОЛЖНА повторяться!**

### 🚨 КРИТИЧНО: Репозиторий и рабочая папка ОДНА И ТА ЖЕ!

**Репозиторий Git находится в `/home/idesig02/fetr.in.ua/www/`**

**В корень `/home/idesig02/fetr.in.ua/` НИЧЕГО не сохраняется!**

### 🚨 КРИТИЧНО: Проверка index.html после деплоя

**ПЕРЕД КАЖДЫМ ДЕПЛОЕМ И ПОСЛЕ НЕГО ОБЯЗАТЕЛЬНО ПРОВЕРЯТЬ:**

1. ✅ `www/index.html` скопирован из `dist/index.html`
2. ✅ `www/index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`
3. ✅ В консоли браузера НЕТ ошибки "Failed to load module script: Expected a JavaScript-or-Wasm module script"

**🚨 ЕСЛИ ВИДИТЕ ОШИБКУ "Failed to load module script" - ЗНАЧИТ ЗАБЫЛИ СКОПИРОВАТЬ `dist/index.html` в `www/`!**

**⚠️ ЭТО САМАЯ ЧАСТАЯ ОШИБКА! НЕ ЗАБЫВАЙТЕ КОПИРОВАТЬ index.html В www/ ПОСЛЕ КАЖДОЙ СБОРКИ!**

**Команда проверки:**
```bash
ssh USER@SERVER "cd /home/idesig02/fetr.in.ua/www && tail -5 index.html | grep -E 'main\.tsx|index-.*\.js'"
```

**Должно быть:** `/assets/index-*.js` (скомпилированный файл)  
**НЕ должно быть:** `/src/main.tsx` (исходный файл)

**Команда исправления:**
```bash
ssh USER@SERVER "cd /home/idesig02/fetr.in.ua/www && cp dist/index.html index.html && echo '=== INDEX.HTML FIXED ==='"
```

---

## Команды сборки

**⚠️ ВАЖНО:** Используется `npm run build` для продакшена!

```bash
# Разработка
npm run dev

# Продакшн сборка
npm run build

# Предпросмотр продакшн сборки
npm run preview

# Линтинг
npm run lint
```

**После сборки ОБЯЗАТЕЛЬНО:**
1. Проверить что `dist/` создан
2. Проверить что `dist/index.html` ссылается на `/assets/index-*.js`
3. Коммитить `dist/` в Git (если изменился фронтенд)

---

## Frontend

### Технологии:
- **React 18** + **TypeScript**
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **[shadcn/ui](https://ui.shadcn.com/)** - UI компоненты (ПРИОРИТЕТ #1)
- **[shadcn/ui Blocks](https://ui.shadcn.com/blocks)** - готовые блоки для админки и других разделов (ПРИОРИТЕТ #2)
- **React Router** - роутинг
- **React Query** - работа с данными
- **React Hook Form** + **Zod** - формы и валидация

### Структура:
- `/src/components/` - React компоненты
  - `/cart/` - компоненты корзины
  - `/checkout/` - страницы оформления заказа
  - `/layout/` - компоненты макета (Header, Footer)
  - `/products/` - компоненты товаров
  - `/sections/` - секции главной страницы
  - `/ui/` - UI компоненты из shadcn-ui
- `/src/contexts/` - React контексты
- `/src/data/` - статические данные
- `/src/hooks/` - кастомные хуки
- `/src/lib/` - утилиты
- `/src/pages/` - страницы приложения
- `/src/types/` - TypeScript типы

### Правила кодирования:
- Компоненты в PascalCase, файлы в PascalCase.tsx
- Хуки начинаются с `use`
- Используй алиас `@/` для импортов из `src/`
- Все UI компоненты должны быть в `src/components/ui/`
- Страницы в `src/pages/`

### 🎨 КРИТИЧНО: Использование UI компонентов

**ПРИОРИТЕТ ВСЕГДА: Готовые компоненты из shadcn/ui**

#### Порядок выбора компонентов:

1. **ПЕРВЫЙ ПРИОРИТЕТ** - Готовые компоненты из [shadcn/ui Components](https://ui.shadcn.com/docs/components)
   - Проверить список доступных компонентов
   - Установить через `npx shadcn add <component-name>`
   - Использовать готовый компонент

2. **ВТОРОЙ ПРИОРИТЕТ** - Готовые блоки из [shadcn/ui Blocks](https://ui.shadcn.com/blocks)
   - Проверить готовые блоки (dashboard, sidebar, forms и т.д.)
   - Установить через `npx shadcn add <block-name>`
   - Адаптировать под нужды проекта

3. **ТРЕТИЙ ПРИОРИТЕТ** - Комбинирование существующих компонентов
   - Использовать комбинацию готовых компонентов из shadcn/ui
   - Минимальная кастомизация через props и className

4. **ПОСЛЕДНИЙ ПРИОРИТЕТ** - Самописные компоненты
   - **ЗАПРЕЩЕНО** писать самописные компоненты без письменного подтверждения!
   - Перед написанием самописного компонента ОБЯЗАТЕЛЬНО:
     1. Проверить, что нужного компонента нет в [shadcn/ui Components](https://ui.shadcn.com/docs/components)
     2. Проверить, что нет подходящего блока в [shadcn/ui Blocks](https://ui.shadcn.com/blocks)
     3. Убедиться, что нельзя решить задачу комбинацией существующих компонентов
     4. **Получить письменное подтверждение** от пользователя
     5. Только после подтверждения писать самописный компонент

#### Примеры использования:

```bash
# Добавить компонент из shadcn/ui
npx shadcn add button
npx shadcn add card
npx shadcn add dialog

# Добавить готовый блок из shadcn/ui blocks
npx shadcn add dashboard-01
npx shadcn add sidebar-07
npx shadcn add login-03
```

#### Для админ-панели:

**ИСПОЛЬЗОВАТЬ готовые блоки из [shadcn/ui blocks](https://ui.shadcn.com/blocks):**
- `dashboard-01` - готовый дашборд с графиками и таблицей
- `sidebar-07` или `sidebar-03` - готовые сайдбары
- Готовые таблицы с сортировкой/фильтрацией
- Готовые формы для редактирования

**НЕ писать админ-компоненты с нуля!**

---

## Deployment

### ✅ ПРАВИЛЬНЫЙ способ (Git Deploy - автоматический):

**После изменений фронтенда:**
```powershell
# 1. Собрать проект
npm run build

# 2. Добавить изменения (включая dist/)
git add .
git commit -m "Описание изменений"
git push origin main

# 3. На сервере выполнить деплой (см. deploy-rules.md)
```

**Что происходит автоматически на сервере:**
1. Обновление кода (`git fetch && git reset --hard origin/main`)
2. Копирование `dist/index.html` → `index.html` (КРИТИЧНО!)
3. Копирование `dist/assets/*` → `assets/`
4. Установка прав доступа (755 для папок, 644 для файлов)

### ❌ Устаревший способ (НЕ использовать):

~~Загрузка через FTP~~ - только для экстренных случаев

### Проверка после деплоя:
1. **Ctrl+Shift+R** в браузере
2. DevTools → Network → проверить что загружаются `/assets/index-*.js`
3. DevTools → Console → проверить отсутствие ошибок

---

## 🔐 Права доступа к файлам (КРИТИЧНО!)

### Обязательные права после каждого деплоя:

**После копирования файлов из `dist/` ОБЯЗАТЕЛЬНО установить права:**

```bash
# Папки: 755 (drwxr-xr-x) - веб-сервер может читать и выполнять
chmod 755 assets/
chmod 755 public/

# Файлы: 644 (rw-r--r--) - веб-сервер может читать
chmod 644 index.html
chmod 644 assets/*
chmod 644 public/*
```

**Почему это важно:**
- Без правильных прав браузер получит `403 Forbidden` для CSS/JS файлов
- Сайт будет показывать белую страницу
- Проблема возникает после каждого деплоя, если права не установлены

---

## Важные заметки

1. **Git workflow**: Всегда делать `npm run build` перед коммитом, если изменен фронтенд.

2. **Деплой**: 
   - **КРИТИЧНО:** Репозиторий и рабочая папка ОДНА И ТА ЖЕ: `/home/idesig02/fetr.in.ua/www/`
   - **КРИТИЧНО:** Всегда копировать `dist/index.html` в `index.html` после `git pull` и сборки
   - **КРИТИЧНО:** В корень `/home/idesig02/fetr.in.ua/` НИЧЕГО не сохраняется!
   - **КРИТИЧНО:** Проверять, что `index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`
   - Всегда устанавливать права доступа после копирования файлов

3. **Права доступа**: После каждого деплоя обязательно устанавливать права 755 для папок и 644 для файлов, иначе сайт не загрузится (403 Forbidden).

4. **Проверка index.html**: 
   - **ВСЕГДА** проверять после деплоя, что `index.html` не ссылается на `/src/main.tsx`
   - Если видите ошибку "Failed to load module script" - значит забыли скопировать `dist/index.html` в `index.html`
   - Команда проверки: `cd /home/idesig02/fetr.in.ua/www && grep -E 'main\.tsx|index-.*\.js' index.html`

5. **Тестирование**: После деплоя всегда проверять:
   - Сайт открывается без ошибок
   - Консоль браузера не показывает ошибки 403 или "Failed to load module script"
   - Все ресурсы загружаются корректно

---

---

## 📸 Компонент ImageLightbox

**КРИТИЧЕСКИ ВАЖНО:** Использовать универсальный компонент `ImageLightbox` для всех всплывающих галерей изображений на сайте.

### ✅ ОБЯЗАТЕЛЬНО ИСПОЛЬЗОВАТЬ ImageLightbox

**Для всех случаев, когда нужно показать полноэкранную галерею изображений:**
- ✅ Галерея товаров в ProductModal
- ✅ Галерея материалов в ProductModal
- ✅ Галерея на главной странице (GallerySection)
- ✅ Любые другие галереи изображений

**ЗАПРЕЩЕНО:** Писать отдельные компоненты галереи для каждого блока!

### Использование ImageLightbox

```tsx
import { ImageLightbox, ImageLightboxItem } from '@/components/ImageLightbox';

const [isOpen, setIsOpen] = useState(false);
const [currentIndex, setCurrentIndex] = useState(0);

// Преобразуем данные в формат ImageLightboxItem[]
const lightboxImages: ImageLightboxItem[] = images.map(url => ({ url }));
// Или для более сложных данных:
const lightboxImages: ImageLightboxItem[] = items.map(item => ({
  url: item.image,
  title: item.name,
  description: item.description || undefined,
}));

// Использование компонента
<ImageLightbox
  isOpen={isOpen}
  images={lightboxImages}
  initialIndex={currentIndex}
  onClose={() => setIsOpen(false)}
  zIndex={50} // Для управления z-index при вложенных модалках
/>
```

### Функциональность ImageLightbox

Компонент автоматически поддерживает:
- ✅ Навигацию между изображениями (кнопки влево/вправо)
- ✅ Закрытие по клику на фон или кнопку X
- ✅ Закрытие по клавише Escape
- ✅ Свайп на мобильных устройствах (влево/вправо)
- ✅ Отображение номера изображения (X / Total)
- ✅ Поддержку title и description для каждого изображения
- ✅ Правильные z-index (кнопки z-20, изображение z-10)

### Пример замены старого кода

**БЫЛО (неправильно - отдельная галерея):**
```tsx
{isLightboxOpen && (
  <div className="fixed inset-0 z-50 ...">
    <button onClick={closeLightbox}>X</button>
    <button onClick={prevImage}>←</button>
    <button onClick={nextImage}>→</button>
    <img src={images[currentIndex]} />
  </div>
)}
```

**СТАЛО (правильно - универсальный компонент):**
```tsx
<ImageLightbox
  isOpen={isLightboxOpen}
  images={images.map(url => ({ url }))}
  initialIndex={currentIndex}
  onClose={() => setIsLightboxOpen(false)}
  zIndex={50}
/>
```

**Последнее обновление:** 2026-01-17


