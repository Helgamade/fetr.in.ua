# Правила деплоя для fetr.in.ua

## 🚨🚨🚨 КРИТИЧЕСКИ ВАЖНОЕ ПРАВИЛО 🚨🚨🚨

**В КОНЦЕ КАЖДОГО ДЕПЛОЯ ОБЯЗАТЕЛЬНО КОПИРОВАТЬ ФАЙЛЫ ИЗ `dist/` В КОРЕНЬ!**

**ЭТО ПРАВИЛО НИКОГДА НЕЛЬЗЯ НАРУШАТЬ!**

**ВСЕГДА ВЫПОЛНЯЙ:**
```bash
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && bash server/deploy.sh"
```

**Или вручную:**
```bash
cp dist/index.html index.html              # ПЕРВЫМ!
cp -r dist/assets/* assets/
chmod 755 assets && chmod 644 assets/* && chmod 644 index.html
```

**БЕЗ КОПИРОВАНИЯ ФАЙЛОВ САЙТ НЕ РАБОТАЕТ!**

---

## 🚀 Как деплоить изменения на production сервер

### ✅ ПРАВИЛЬНЫЙ способ (Git Deploy):

После любых изменений в коде **ВСЕГДА** используй эту последовательность команд:

```powershell
# 1. Если изменен фронтенд - собрать проект
npm run build

# 2. Добавить все изменения (включая dist/ если изменился фронтенд)
git add .

# 3. Закоммитить с описанием
git commit -m "Описание изменений"

# 4. Загрузить в GitHub (бэкап)
git push origin main

# 5. На сервере выполнить деплой (см. команды ниже)
```

---

## 🔄 Что происходит при деплое:

### Ручной деплой (через скрипт deploy.sh):

**Всегда используй скрипт `server/deploy.sh` для деплоя!**

```bash
cd /home/idesig02/fetr.in.ua/www
bash server/deploy.sh
```

**Скрипт выполняет следующие действия (В ТАКОМ ПОРЯДКЕ):**

1. **Сборка фронтенда:**
   ```bash
   npm run build
   ```

2. **КРИТИЧНО: Копирование файлов из `dist/` в корень:**
   ```bash
   mkdir -p assets
   cp dist/index.html index.html              # ПЕРВЫМ! Без этого сайт не загрузится!
   cp -r dist/assets/* assets/                # Копирование JS/CSS файлов
   cp dist/favicon.ico favicon.ico            # Если есть
   cp dist/robots.txt robots.txt              # Если есть
   ```

3. **Установка прав доступа (ОБЯЗАТЕЛЬНО!):**
   ```bash
   chmod 755 assets
   chmod 644 assets/*
   chmod 644 index.html
   ```

4. **Перезапуск сервера:**
   ```bash
   cd server
   pkill -f "node.*index.js"
   nohup node index.js > /dev/null 2>&1 &
   ```

---

## 📋 Быстрая команда для ручного деплоя:

**Если нужно деплоить вручную на сервере:**

```bash
cd /home/idesig02/fetr.in.ua/www
bash server/deploy.sh
```

**Или через SSH одной командой:**

```powershell
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua/www && bash server/deploy.sh"
```

---

## 🔄 Автоматическое копирование файлов

**КРИТИЧНО: Git hooks автоматически копируют файлы после каждого git pull/reset**

На сервере настроены Git hooks:
- `.git/hooks/post-merge` - выполняется после `git pull` или `git merge`
- `.git/hooks/post-checkout` - выполняется после `git reset --hard` или `git checkout`

Оба hook вызывают `server/auto-deploy.sh`, который:
1. Копирует `dist/index.html` → `index.html`
2. Копирует `dist/assets/*` → `assets/`
3. Проверяет, что файлы скопированы правильно (нет ссылок на `main.tsx`)
4. Устанавливает права доступа

**Это гарантирует, что файлы ВСЕГДА будут скопированы после обновления кода!**

---

## 🚨 КРИТИЧНО: Файлы ДОЛЖНЫ копироваться из dist/

**После каждого деплоя (автоматического или ручного) ОБЯЗАТЕЛЬНО:**

1. ✅ `dist/index.html` → `index.html` (ПЕРВЫМ!)
2. ✅ `dist/assets/*` → `assets/`
3. ✅ Установка прав доступа

**Проверка после деплоя:**
- Проверить что `index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`
- В консоли браузера НЕТ ошибки "Failed to load module script"
```

---

## 🔐 Настройки Git Deploy:

### Production remote:
- **SSH хост:** `idesig02@idesig02.ftp.tools`
- **Репозиторий:** `~/deploy.git` (bare)
- **Рабочая директория:** `/home/idesig02/fetr.in.ua`
- **Hook:** `~/deploy.git/hooks/post-receive`

### Проверка настройки:

```powershell
git remote -v
```

---

## 🐛 Устранение проблем:

### Проблема 1: "Failed to load module script: Expected a JavaScript-or-Wasm module script"

**Причина**: На сервере старый `index.html`, который ссылается на `/src/main.tsx` вместо скомпилированного JS из `dist/assets/`.

**Решение:**
```bash
# КРИТИЧНО: Скопировать правильный index.html из dist/
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && cp dist/index.html index.html && echo '=== INDEX.HTML FIXED ==='"

# Проверить, что index.html правильный
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && tail -5 index.html"
# Должно быть: <script type="module" crossorigin src="/assets/index-*.js"></script>
# НЕ должно быть: <script type="module" src="/src/main.tsx"></script>
```

**Проверка:**
```bash
# Проверить содержимое index.html
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && grep -E 'main\.tsx|index-.*\.js' index.html"
# Должно быть: /assets/index-*.js (скомпилированный файл)
# НЕ должно быть: /src/main.tsx (исходный файл)
```

---

### Проблема 2: "403 Forbidden" для CSS/JS файлов (белая страница)

**Причина**: Неправильные права доступа к папке `assets/` или файлам в ней.

**Симптомы:**
- Браузер показывает ошибки: `GET /assets/index-*.css net::ERR_ABORTED 403`
- Браузер показывает ошибки: `GET /assets/index-*.js net::ERR_ABORTED 403`
- Страница полностью белая, консоль показывает только ошибки 403

**Решение:**
```bash
# Установить правильные права для assets/ и файлов
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && chmod 755 assets && chmod 644 assets/* && chmod 644 index.html && echo '=== PERMISSIONS FIXED ==='"
```

**Проверка:**
```bash
# Проверить права доступа
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && ls -la assets/"
# Должно быть: drwxr-xr-x для папки, -rw-r--r-- для файлов
```

---

### Проблема 3: "fatal: Need to specify how to reconcile divergent branches"

**Причина**: На сервере есть локальные изменения, которые конфликтуют с удаленной веткой.

**Решение:**
```bash
# Использовать жесткий reset
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && git fetch origin && git reset --hard origin/main"
```

---

### Проблема 4: Файлы не обновляются на сайте

**Причина**: Кеш браузера или файлы не скопированы.

**Решение:**
```bash
# 1. Проверить, что файлы обновились на сервере
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && ls -la dist/assets/ | head -5"

# 2. Проверить, что index.html обновлен
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && head -5 index.html"

# 3. Если файлы не обновились - повторить деплой
```

**Сообщить пользователю**:
> "Файлы на сервере обновлены, но сайт показывает старую версию. Очистите кеш браузера (Ctrl+Shift+R) или проверьте настройки кеширования на сервере."

---

### Проблема 5: "Permission denied (publickey)"

**Причина**: SSH ключ не настроен или неверный.

**Решение**: Сообщить пользователю:
> "Не удалось подключиться к серверу по SSH. Проверьте:
> 1. SSH ключ добавлен в `~/.ssh/authorized_keys` на сервере
> 2. Права доступа к ключу: `chmod 600 ~/.ssh/id_rsa`
> 3. SSH ключ добавлен в ssh-agent: `ssh-add ~/.ssh/id_rsa`"

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ:

Если деплой не работает, выполнить диагностику:

```bash
# 1. Проверьте последние коммиты на сервере
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && git log --oneline -5"

# 2. Проверьте статус Git на сервере
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && git status"

# 3. Проверьте права доступа
ssh idesig02@idesig02.ftp.tools "ls -la /home/idesig02/fetr.in.ua/"

# 4. Проверьте содержимое index.html
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && cat index.html | grep -E 'main\.tsx|index-.*\.js'"
```

---

## 🔄 ОТКАТ ИЗМЕНЕНИЙ:

Если что-то пошло не так и нужно откатить:

```bash
# 1. Посмотреть последние коммиты
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && git log --oneline -5"

# 2. Откатить на предыдущий коммит (ОСТОРОЖНО!)
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && git reset --hard HEAD~1"

# 3. Или откатить на конкретный коммит
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && git reset --hard <commit-hash>"

# 4. После отката скопировать файлы заново
ssh idesig02@idesig02.ftp.tools "cd /home/idesig02/fetr.in.ua && cp dist/index.html index.html && cp -r dist/assets/* assets/ && chmod 755 assets && chmod 644 assets/* && chmod 644 index.html"
```

---

## ✅ ЧЕКЛИСТ УСПЕШНОГО ДЕПЛОЯ:

**🚨 ПЕРВЫЙ ПРИОРИТЕТ - index.html:**

- [ ] `npm run build` выполнен успешно
- [ ] **🚨🚨🚨 `cp dist/index.html index.html` выполнено ПЕРВЫМ (КРИТИЧНО! Без этого сайт НЕ ЗАГРУЗИТСЯ!)** 🚨🚨🚨
- [ ] **🚨 Проверено, что `index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`** 🚨
- [ ] Изменения закоммичены и запушены в Git
- [ ] `git reset --hard origin/main` выполнен на сервере без ошибок
- [ ] `cp -r dist/assets/* assets/` выполнен (если изменился фронтенд)
- [ ] **`chmod 755 assets && chmod 644 assets/* && chmod 644 index.html` выполнено (ОБЯЗАТЕЛЬНО!)** 
- [ ] Сайт открывается: `curl https://fetr.in.ua/ | head -30`
- [ ] Новые файлы видны на сайте (очистить кеш браузера)
- [ ] Нет ошибок 403 Forbidden в консоли браузера
- [ ] **🚨 НЕТ ОШИБКИ "Failed to load module script: Expected a JavaScript-or-Wasm module script" в консоли браузера** 🚨

---

**Последнее обновление:** 2025-01-XX  
**Версия:** 1.0

