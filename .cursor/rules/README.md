# Правила проекта FetrInUA

Эта папка содержит правила и инструкции для разработки и деплоя проекта.

## Структура файлов:

- **DEPLOYMENT_GUIDE.md** - ⭐ **ПОЛНАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ** (для агентов и разработчиков)
- **project-rules.md** - Основные правила проекта, структура, стандарты кодирования
- **deploy-rules.md** - Подробные инструкции по деплою на сервер
- **build-config.md** - Конфигурация сборки проекта
- **setup-deployment.md** - Настройка автоматического деплоя

## Быстрый старт:

1. **Разработка:**
   ```bash
   npm run dev
   ```

2. **Сборка для продакшена:**
   ```bash
   npm run build
   ```

3. **Деплой на сервер:**
   - ⭐ **См. полную инструкцию в `DEPLOYMENT_GUIDE.md`** (для агентов)
   - См. подробные инструкции в `deploy-rules.md`
   - Используйте скрипт `server/deploy.sh` на сервере

## Критически важно:

🚨 **ВСЕГДА** репозиторий и рабочая папка ОДНА И ТА ЖЕ: `/home/idesig02/fetr.in.ua/www/`

🚨 **ВСЕГДА** копировать `dist/index.html` → `index.html` после деплоя!

🚨 **ВСЕГДА** в корень `/home/idesig02/fetr.in.ua/` НИЧЕГО не сохраняется!

🚨 **ВСЕГДА** устанавливать права доступа: `chmod 755 assets && chmod 644 assets/*`

🚨 **ВСЕГДА** проверять что `index.html` ссылается на `/assets/index-*.js`, а НЕ на `/src/main.tsx`

## Полезные команды:

```bash
# Проверка index.html на сервере
ssh USER@SERVER "cd /home/idesig02/fetr.in.ua/www && grep -E 'main\.tsx|index-.*\.js' index.html"

# Быстрый деплой
ssh USER@SERVER "cd /home/idesig02/fetr.in.ua/www && git fetch origin && git reset --hard origin/main && npm install && npm run build && cp dist/index.html index.html && cp -r dist/assets/* assets/ && chmod 755 assets && chmod 644 assets/* && chmod 644 index.html"
```

## Документация:

- ⭐ **[Полная инструкция по деплою](./DEPLOYMENT_GUIDE.md)** - для агентов и разработчиков
- [Правила проекта](./project-rules.md)
- [Правила деплоя](./deploy-rules.md)
- [Конфигурация сборки](./build-config.md)
- [Настройка деплоя](./setup-deployment.md)


