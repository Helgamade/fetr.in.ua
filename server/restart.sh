#!/bin/bash
# Скрипт для перезапуска API сервера

cd "$(dirname "$0")/.."

echo "🔄 Restarting server..."

# Останавливаем сервер
./server/stop.sh 2>/dev/null || true

# Ждем завершения
sleep 2

# Запускаем заново
./server/start.sh

